What happens when you initialize a repository? Why do you need to do it?

initialization creates the .git folder, which contains all data need to git working. We need it to use version-control, it contain log file also.